# Production Environment Configuration for tools.veerl.es
# Copy this to .env on your production server

APP_ENV=production

# Database Configuration
DB_HOST=127.0.0.1
DB_NAME=loginveerles
DB_USER=your_db_user
DB_PASS=your_db_password
DB_CHARSET=utf8mb4

# JWT/Cookie Settings
JWT_COOKIE_NAME=sso_token
JWT_COOKIE_DOMAIN=tools.veerl.es
JWT_LIFETIME_MINUTES=60
COOKIE_NAME=appsess
COOKIE_SECURE=true
COOKIE_SAMESITE=Lax
COOKIE_PATH=/
SESSION_LIFETIME_MINUTES=30
REMEMBER_DAYS=30

# Application URLs
APP_BASE_URL=https://tools.veerl.es/apps/auth

# Email Configuration
MAIL_FROM=no-reply@veerl.es

# SMTP Settings
SMTP_HOST=mail.veerl.es
SMTP_PORT=465
SMTP_USERNAME=no-reply@veerl.es
SMTP_PASSWORD=xhnk%mVu+]{(2Oby
SMTP_SECURE=ssl
SMTP_TIMEOUT=30
